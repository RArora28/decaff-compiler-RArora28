class Program {
	int main() {
		int n;
		n = read(); 
		int arr[n]; 
		for (i = 0, n) {
			arr[i] = read(); 
			if (arr[i] % 2 == 0) {
				callout("printf", "%d ", arr[i]); 
			}
		}
		callout("printf", "\n"); 
		return 0; 
	}
}